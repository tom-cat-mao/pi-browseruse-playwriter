---
'playwriter': patch
---

Accept the fork extension identity (`eeklahpecooapnailfaebkjjembkjhhg`) in the relay CORS, `/cdp` and `/extension` origin allowlists, next to the legacy Chrome Web Store and dev extension ids from `ALLOWED_EXTENSION_IDS`.
