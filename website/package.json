{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "sigillo run -- vite --port 8044",
    "typecheck": "tsc",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:preview": "echo \"D1 pre-migration timestamp: $(date +%s)\" && CI=1 wrangler d1 migrations apply DB --remote --env preview",
    "db:migrate:prod": "echo \"D1 pre-migration timestamp: $(date +%s)\" && CI=1 wrangler d1 migrations apply DB --remote",
    "secrets": "pnpm run secrets:preview && pnpm run secrets:prod",
    "secrets:preview": "sigillo run -c preview --mount .env.preview --mount-format env -- wrangler secret bulk --env preview .env.preview",
    "secrets:prod": "sigillo run -c prod --mount .env.prod --mount-format env -- wrangler secret bulk .env.prod",
    "stripe:listen": "tuistory -- stripe listen --live --forward-to http://localhost:8044/api/stripe/webhook",
    "deploy": "CLOUDFLARE_ENV=preview sigillo run -c preview --command 'pnpm db:migrate:preview && tsc && vite build && wrangler deploy --env preview'",
    "deploy:prod": "sigillo run -c prod --command 'pnpm db:migrate:prod && tsc && vite build && wrangler deploy'"
  },
  "dependencies": {
    "@better-auth/api-key": "^1.6.20",
    "@cloudflare/vite-plugin": "^1.42.1",
    "@holocron.so/vite": "^0.29.0",
    "better-auth": "^1.6.20",
    "better-auth-drizzle-adapter": "^1.1.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "db": "workspace:^",
    "drizzle-orm": "1.0.0-beta.22",
    "radix-ui": "^1.6.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "spiceflow": "1.26.0-rsc.5",
    "stripe": "^22.2.2",
    "tailwind-merge": "^3.6.0",
    "ulid": "^3.0.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vite": "^8.0.13",
    "wrangler": "^4.103.0"
  }
}
